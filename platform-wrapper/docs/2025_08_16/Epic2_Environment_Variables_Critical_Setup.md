# Epic 2: Environment Variables Critical Setup
## Railway to Render Migration - Complete Mapping Guide

**Priority**: 🔐 CRITICAL SECURITY  
**Date**: 2025-08-16  
**Focus**: AUTH0_CLIENT_SECRET and Production Configuration

---

## 🚨 CRITICAL SECURITY NOTICE

**AUTH0_CLIENT_SECRET MUST BE SET MANUALLY**  
This secret is NOT included in the blueprint for security reasons and must be configured in Render dashboard immediately after deployment.

---

## 📋 ENVIRONMENT VARIABLES MAPPING

### 🔐 CRITICAL SECURITY VARIABLES (Manual Setup Required)

| Variable | Railway Value | Render Configuration | Priority |
|----------|---------------|---------------------|----------|
| `AUTH0_CLIENT_SECRET` | `9CnJeRKicS44doQi48R12vnTU3aZcEb63dL52okVmVyd5InpUfSQNnMNiQDpEtt2` | **SET MANUALLY IN DASHBOARD** | 🔴 CRITICAL |
| `JWT_SECRET_KEY` | `UyPh4kkCPcGoRSCLswKw15MMT9VBy76p2uFWAe5hthI=` | **AUTO-GENERATED BY RENDER** | 🔴 CRITICAL |

### ✅ AUTO-CONFIGURED VARIABLES (Blueprint Managed)

| Variable | Value | Source | Status |
|----------|-------|---------|---------|
| `PORT` | `80` | Blueprint | ✅ Configured |
| `FASTAPI_PORT` | `8000` | Blueprint | ✅ Configured |
| `ENVIRONMENT` | `production` | Blueprint | ✅ Configured |
| `DEBUG` | `false` | Blueprint | ✅ Configured |
| `LOG_LEVEL` | `INFO` | Blueprint | ✅ Configured |
| `AUTH0_DOMAIN` | `dev-g8trhgbfdq2sk2m8.us.auth0.com` | Blueprint | ✅ Configured |
| `AUTH0_CLIENT_ID` | `mQG01Z4lNhTTN081GHbR9R9C4fBQdPNr` | Blueprint | ✅ Configured |
| `AUTH0_CALLBACK_URL` | `https://marketedge-platform.onrender.com/callback` | Blueprint | ✅ Configured |
| `JWT_ALGORITHM` | `HS256` | Blueprint | ✅ Configured |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | `30` | Blueprint | ✅ Configured |
| `RATE_LIMIT_ENABLED` | `true` | Blueprint | ✅ Configured |
| `RATE_LIMIT_REQUESTS_PER_MINUTE` | `60` | Blueprint | ✅ Configured |

### 🔗 DATABASE VARIABLES (Auto-Connected)

| Variable | Type | Configuration | Status |
|----------|------|---------------|---------|
| `DATABASE_URL` | PostgreSQL | Auto-connected from `marketedge-postgres` | ✅ Auto-managed |
| `REDIS_URL` | Redis | Auto-connected from `marketedge-redis` | ✅ Auto-managed |

### 🌐 CORS CONFIGURATION (Critical for Frontend)

```json
CORS_ORIGINS: ["https://frontend-5r7ft62po-zebraassociates-projects.vercel.app","http://localhost:3000"]
```

**Configured Origins:**
- ✅ Vercel Frontend: `https://frontend-5r7ft62po-zebraassociates-projects.vercel.app`
- ✅ Local Development: `http://localhost:3000`

---

## 🔧 MANUAL SETUP INSTRUCTIONS

### Step 1: AUTH0_CLIENT_SECRET Setup (CRITICAL)

**In Render Dashboard:**

1. **Navigate to Service**
   ```
   Dashboard → Services → marketedge-platform → Environment
   ```

2. **Add Secret Variable**
   ```
   Click "Add Environment Variable"
   
   Key: AUTH0_CLIENT_SECRET
   Value: 9CnJeRKicS44doQi48R12vnTU3aZcEb63dL52okVmVyd5InpUfSQNnMNiQDpEtt2
   
   ✅ Check "Secret" (encrypts value)
   Click "Save Changes"
   ```

3. **Verify Configuration**
   ```
   Environment tab should show:
   AUTH0_CLIENT_SECRET: [SECRET] (hidden value)
   ```

4. **Trigger Redeploy**
   ```
   Manual Deploy → "Deploy Latest Commit"
   ```

### Step 2: Database Connection Verification

**Auto-configured by Blueprint:**

```yaml
# PostgreSQL Database
DATABASE_URL: postgresql://[user]:[password]@[host]:[port]/marketedge_production

# Redis Cache  
REDIS_URL: redis://[user]:[password]@[host]:[port]
```

**Verification Commands:**
```bash
# Check database connection
curl https://marketedge-platform.onrender.com/api/v1/admin/health/database

# Check Redis connection
curl https://marketedge-platform.onrender.com/api/v1/admin/health/redis
```

### Step 3: CORS Validation

**Test CORS Configuration:**
```bash
# Test from Vercel frontend origin
curl -H "Origin: https://frontend-5r7ft62po-zebraassociates-projects.vercel.app" \
     -H "Access-Control-Request-Method: GET" \
     -H "Access-Control-Request-Headers: Content-Type,Authorization" \
     -X OPTIONS \
     https://marketedge-platform.onrender.com/api/v1/health

# Expected Response Headers:
Access-Control-Allow-Origin: https://frontend-5r7ft62po-zebraassociates-projects.vercel.app
Access-Control-Allow-Credentials: true
Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS, HEAD, PATCH
```

---

## 🔍 ENVIRONMENT VARIABLES VALIDATION

### Validation Script

```bash
#!/bin/bash
# Environment Variables Validation Script

echo "🔍 Epic 2 Environment Variables Validation"
echo "=========================================="

# 1. Test Auth0 Configuration
echo "1. Testing Auth0 configuration..."
curl -s https://marketedge-platform.onrender.com/.well-known/openid-configuration | grep -q "dev-g8trhgbfdq2sk2m8.us.auth0.com"
if [ $? -eq 0 ]; then
    echo "✅ AUTH0_DOMAIN: Configured correctly"
else
    echo "❌ AUTH0_DOMAIN: Configuration issue"
fi

# 2. Test Database Connection
echo "2. Testing database connection..."
DB_RESPONSE=$(curl -s https://marketedge-platform.onrender.com/api/v1/admin/health/database)
if echo "$DB_RESPONSE" | grep -q "healthy"; then
    echo "✅ DATABASE_URL: Connected successfully"
else
    echo "❌ DATABASE_URL: Connection issue"
fi

# 3. Test Redis Connection
echo "3. Testing Redis connection..."
REDIS_RESPONSE=$(curl -s https://marketedge-platform.onrender.com/api/v1/admin/health/redis)
if echo "$REDIS_RESPONSE" | grep -q "healthy"; then
    echo "✅ REDIS_URL: Connected successfully"
else
    echo "❌ REDIS_URL: Connection issue"
fi

# 4. Test CORS Configuration
echo "4. Testing CORS configuration..."
CORS_RESPONSE=$(curl -s -H "Origin: https://frontend-5r7ft62po-zebraassociates-projects.vercel.app" \
                     -X OPTIONS \
                     https://marketedge-platform.onrender.com/api/v1/health)
if [ $? -eq 0 ]; then
    echo "✅ CORS_ORIGINS: Configured correctly"
else
    echo "❌ CORS_ORIGINS: Configuration issue"
fi

echo ""
echo "🎯 Validation Complete!"
```

---

## 🚨 CRITICAL SECURITY CHECKLIST

### Pre-Deployment Security
- [ ] **AUTH0_CLIENT_SECRET**: NOT in blueprint (security)
- [ ] **JWT_SECRET_KEY**: Auto-generated by Render (security)
- [ ] **DATABASE_URL**: Auto-managed connection (security)
- [ ] **REDIS_URL**: Auto-managed connection (security)

### Post-Deployment Security
- [ ] **AUTH0_CLIENT_SECRET**: Set manually in dashboard
- [ ] **Environment Variables**: All marked as secrets where appropriate
- [ ] **Database Access**: Restricted to service only
- [ ] **CORS**: Limited to specific origins only

### Security Validation
- [ ] **No secrets in logs**: Check deployment logs
- [ ] **No secrets in repository**: Verify blueprint file
- [ ] **Environment encryption**: Verify secrets are encrypted
- [ ] **Access controls**: Verify dashboard permissions

---

## 🔧 TROUBLESHOOTING GUIDE

### Issue 1: AUTH0_CLIENT_SECRET Not Working

**Symptoms:**
```
401 Unauthorized
"Invalid client credentials"
Auth endpoints failing
```

**Solution:**
```bash
# 1. Verify value in Render Dashboard
# 2. Check value matches Railway backup:
#    9CnJeRKicS44doQi48R12vnTU3aZcEb63dL52okVmVyd5InpUfSQNnMNiQDpEtt2
# 3. Ensure marked as "Secret"
# 4. Redeploy service after setting
```

### Issue 2: Database Connection Failing

**Symptoms:**
```
"Connection refused"
"Database unavailable"
Health checks failing
```

**Solution:**
```bash
# 1. Check DATABASE_URL auto-connection
# 2. Verify PostgreSQL service is running
# 3. Check database service logs
# 4. Validate connection string format
```

### Issue 3: CORS Still Failing

**Symptoms:**
```
"CORS policy error"
OPTIONS requests failing
Frontend cannot connect
```

**Solution:**
```bash
# 1. Verify CORS_ORIGINS format in environment
# 2. Check exact Vercel URL match
# 3. Test with curl commands above
# 4. Check Caddy proxy logs
```

### Issue 4: Redis Connection Issues

**Symptoms:**
```
"Redis unavailable"
Cache operations failing
Rate limiting not working
```

**Solution:**
```bash
# 1. Check REDIS_URL auto-connection
# 2. Verify Redis service is running  
# 3. Test connection with health endpoint
# 4. Check Redis service logs
```

---

## 📊 CONFIGURATION SUMMARY

### Automatic Configuration ✅
- Core application settings (PORT, ENVIRONMENT, etc.)
- Auth0 domain and client ID
- CORS origins including Vercel frontend
- Database and Redis auto-connections
- Rate limiting configuration
- Security headers and JWT algorithm

### Manual Configuration Required 🔐
- **AUTH0_CLIENT_SECRET** (critical security requirement)

### Validation Required ✅
- Health endpoint responding
- Auth0 integration working
- Database migrations completed
- Redis cache operational
- CORS allowing frontend connections

---

**🎯 ENVIRONMENT SETUP COMPLETE**  
Once AUTH0_CLIENT_SECRET is manually configured, all environment variables will be properly set for production deployment.

**Next Steps:**
1. Set AUTH0_CLIENT_SECRET in Render dashboard
2. Run validation script
3. Test frontend connectivity
4. Complete Epic 2 deployment